# Security Overrides

Overrides in `package.json` that force safe versions of transitive dependencies.
Each entry is removed automatically by the resolve-renovate skill when `npm audit` is clean for that package.

_No active security overrides._

The `browserslist` override (CVE-2026-73088 / GHSA-73wf-gq98-2v4g, Dependabot alert #75)
was removed on 2026-09-21: the dependency tree now resolves `browserslist` to 4.28.8
(above the 4.28.7 safe floor) without the override, and `npm audit` is clean.
